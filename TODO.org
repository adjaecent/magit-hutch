* TODO Batch apply with invalidation graph
  Mark suggestions for accept (instead of immediate apply), then bulk-apply
  all marked patches bottom-to-top within each file so line numbers don't
  shift. Track an invalidation graph: overlapping/adjacent patches in the
  same file are connected, applying one flags others as stale.

* TODO Send manifest instead of full diffs
  Send a list of changed files/hunks as a manifest and let the LLM use
  tools to read specific diffs on demand. Reduces context usage for large
  changesets.

* TODO Fix global state race in submit_review
  hutch--submitted-findings is global. Concurrent scope reviews can
  overwrite each other's findings. Use a per-scope callback or closure
  to capture findings instead.
