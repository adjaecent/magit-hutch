#+title: magit-hutch: code-review agent for emacs

=magit-hutch= is a code-review agent that runs inside emacs. Invoke it from a magit buffer and get a granular review around the files changed. This can be used as a developer-local review tool before code is published to forges, or other external review harnesses.

It sends a manifest of changed files (not full diffs) and lets the agent fetch what it needs. The agent has access to the following tools:

1. =read_diff= — fetch the full diff for a specific file
2. =search_codebase= — git grep for patterns
3. =read_file= — read file contents (optionally a line range)
4. =git_log= — recent commit history for a file
5. =git_blame= — blame for a file or line range
6. =surrounding_context= — tree-sitter enclosing definition lookup

These tools are run with baked-in guardrails to not overburden the context length. In the future, this could be even tighter by letting the primary agent invoke sub-agents with focussed purposes, possibly in the direction of [[https://arxiv.org/abs/2512.24601][RLMs]].

** Installation

It uses the =gptel= package under the hood, so a =hutch-provider= must be configured beforehand. We recommend using SOTA models only, since the output from cheaper models can be unreliable.

*** straight.el
#+begin_src emacs-lisp
  (use-package magit-hutch
    :straight (:host github :repo "adjaecent/magit-hutch")
    :config
    (setq hutch-backend
          (gptel-make-anthropic "Claude"
            :key (getenv "ANTHROPIC_API_KEY")
            :models '(claude-sonnet-4-6))))
#+end_src

*** elpaca
#+begin_src emacs-lisp
  (use-package magit-hutch
    :ensure (:host github :repo "adjaecent/magit-hutch")
    :config
    (setq hutch-backend
          (gptel-make-anthropic "Claude"
            :key (getenv "ANTHROPIC_API_KEY")
            :models '(claude-sonnet-4-6))))
#+end_src

*** package-vc (Emacs 29+ built-in, no extra deps):
#+begin_src emacs-lisp
  (unless (package-installed-p 'magit-hutch)
    (package-vc-install "https://github.com/adjaecent/magit-hutch"))

  (with-eval-after-load 'magit-hutch
    (setq hutch-backend
          (gptel-make-anthropic "Claude"
            :key (getenv "ANTHROPIC_API_KEY")
            :models '(claude-sonnet-4-6))))
#+end_src

*** Manual clone
#+begin_src emacs-lisp
  (add-to-list 'load-path "~/path/to/magit-hutch")
  (require 'magit-hutch)
  ;; then install magit, gptel, svg-lib, dash via your usual mechanism
  (setq hutch-backend ...)
#+end_src

** Backend configuration
#+begin_src emacs-lisp
  ;; OpenAI
  (gptel-make-openai "OpenAI" :key (getenv "OPENAI_API_KEY") :models '(gpt-5.5))

  ;; OpenRouter (gateway)
  (gptel-make-openai "OpenRouter"
    :host "openrouter.ai" :endpoint "/api/v1/chat/completions"
    :key (getenv "OPENROUTER_API_KEY")
    :models '(deepseek/deepseek-chat))

(use-package magit-hutch
  :straight (:local-repo "/Users/kitallis/Code/adjaecent/magit-hutch")
  :config
  (require 'llm-claude)
  (setq hutch-provider
        (make-llm-claude :key (getenv "ANTHROPIC_API_KEY")
                         :chat-model "claude-sonnet-4-5-20250929")))
#+end_src

** Configuration
#+begin_src emacs-lisp
(use-package magit-hutch
  :straight (:host github :repo "adjaecent/magit-hutch")
  :config
  (setq gptel-log-level 'info)
  (setq hutch-backend
        (gptel-make-openai "OpenAI"
          :key (getenv "OPENAI_API_KEY")
          :models '(gpt-5.5)))
  ;; staged: review staged changes
  ;; unpushed: commits not yet pushed upstream
  ;; branch: commits in this branch not merged against default branch (main, master etc.)
  (setq hutch-scopes '(:staged :branch))
  ;; number of rounds before the agent gives up, 20 is default
  (setq hutch-max-tool-rounds 20)
  ;; tools available to use, default: only read_diff
  (setq hutch-enabled-tools '(read-file search-codebase git-log git-blame surrounding-context)))

#+end_src

** Usage

From any magit buffer, press =d R= to run a review on your changes.

** Roadmap

*** Persistent reviews
Persist reviews under =refs/hutch/<branch>= with a session id at commit time. Past review findings are fed back as context: the model sees "last review flagged X in this file, author accepted/rejected it." This prevents re-flagging dismissed issues, catches recurring patterns, and detects whether previous suggestions were actually addressed.

*** Context graph
A dependency graph of the codebase built lazily as the agent explores. Nodes are files/functions with content hashes, edges are relationships (calls, imports, tests). Used for three things: fetching relevant context (walk forward edges), invalidating stale cache (walk reverse edges), and grouping related files for joint review (connected components). Persisted to =refs/hutch/<branch>= between runs.

*** Composable tool primitives
Instead of hardcoding every tool, expose primitives (git, tree-sitter, file I/O) and let the model compose them. The model writes elisp in a sandboxed eval to build custom queries on the fly — "find all callers of this function that don't handle the nil case." Tools that generate tools.

** LLM-usage disclosure

Most of the UI code is written in an AI-assisted way, but carefully examined. The rest of the agent and operational code is largely hand-written.
